config:
  bind: 0.0.0.0:25565
  onlineMode: true
  servers:
    server1: server1:25565
  try:
    - server1
  forwarding:
    mode: legacy
  status:
    motd: |
      §bGate + geyserlite Cross-Play
      §eJava & Bedrock Players Welcome!

  # Simplest managed setup: Gate starts geyserlite with defaults.
  bedrock: true

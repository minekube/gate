config:
  bind: 0.0.0.0:25565
  onlineMode: true
  servers:
    server1: 0.0.0.0:25566
  try:
    - server1
  # forwarding:
  # mode: legacy
  # velocitySecret: 'bedrock-crossplay-secret'
  status:
    motd: |
      §bGate + Geyser Cross-Play
      §eJava & Bedrock Players Welcome!

  # Enable Bedrock edition support (Geyser + Floodgate)
  bedrock:
    enabled: true
    config:
      # Gate ↔ Geyser connection
      geyserListenAddr: '0.0.0.0:25567'
      # Bedrock player settings
      usernameFormat: '.%s'
      # Floodgate authentication key
      floodgateKeyPath: '.examples/bedrock/geyser/key.pem'
      # Managed Geyser (Gate handles Geyser process automatically)
      managed:
        enabled: true

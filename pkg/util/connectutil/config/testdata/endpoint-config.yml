# This is a simplified config where the rest of the
# settings are omitted and will be set by default.
# See config.yml for the full configuration options.
config:
  bind: 0.0.0.0:25566
  servers:
    mcserver: localhost:25567
  try:
    - mcserver

connect:
  enabled: true
  insecure: true
  name: proxy
  watchServiceAddr: ws://localhost:8080/watch

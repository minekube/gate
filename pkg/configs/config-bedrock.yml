config:
  bind: 0.0.0.0:25565
  onlineMode: true
  servers:
    server1: 0.0.0.0:25566
  try:
    - server1
  # forwarding:
  # mode: legacy
  # velocitySecret: 'bedrock-crossplay-secret'
  status:
    motd: |
      §bGate + Geyser Cross-Play
      §eJava & Bedrock Players Welcome!

  # Enable Bedrock edition support with managed geyserlite defaults.
  bedrock: true

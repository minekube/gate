// Code generated by gate-wasm-gen; DO NOT EDIT.
package minekube:gate@0.1.0;

interface gate-types {
  record gate-error {
    kind: string,
    message: string,
    operation: string,
  }

  resource context;

  resource handler;

  resource proxy;

  record sample {
    text: string,
    tags: option<list<string>>,
  }
}

interface pkg-shapes {
  use gate-types.{gate-error, handler, proxy, sample};

  register: func(handler: option<own<handler>>) -> result<_, gate-error>;
  transform: func(proxy: option<borrow<proxy>>, input: sample) -> result<sample, gate-error>;
}

interface plugin {
  use gate-types.{context, gate-error, handler, proxy};

  record plugin-metadata {
    name: string,
    version: string,
    contract-hash: string,
    generator-format: u32,
  }

  metadata: func() -> plugin-metadata;
  init: func(context: borrow<context>, proxy: borrow<proxy>) -> result<_, gate-error>;
  invoke-handler: func(id: u64, input: string) -> bool;
}

world gate-plugin {
  import pkg-shapes;
  export plugin;
}
